enum BookingServiceType { consultations, litigation }

enum BookingTabType { current, underReview, previous }

enum UserType { client, lawyer }

enum BookingFlow { caseBooking, consultationBooking }

enum ServiceType { consultation, legalRepresentation }

enum Gender { male, female }
